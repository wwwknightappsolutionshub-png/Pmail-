/** Default tenant when visiting /login (production: Prohost Cloud). */
export const DEFAULT_TENANT_SLUG = "prohost";

/** Local PMail+ demo tenant — skips real mail provider verification. */
export const PMAIL_TESTER_TENANT_SLUG = "pmail-tester";
